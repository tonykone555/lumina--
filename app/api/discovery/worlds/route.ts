import {NextRequest,NextResponse} from "next/server";import {listWorlds,loadWorld,owner,saveWorld} from "@/lib/discovery/worlds";export async function GET(req:NextRequest){try{const o=owner(req),id=req.nextUrl.searchParams.get("id");return NextResponse.json(id?await loadWorld(o,id):{worlds:await listWorlds(o)})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Worlds unavailable"},{status:500})}}export async function POST(req:NextRequest){try{const b=await req.json(),profiles=Array.isArray(b.profiles)?b.profiles.slice(0,500):[];if(!String(b.query||"").trim())return NextResponse.json({error:"Query required"},{status:400});return NextResponse.json({world:await saveWorld(owner(req),String(b.title||b.query).slice(0,80),String(b.query).slice(0,180),profiles)})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"World save failed"},{status:500})}}