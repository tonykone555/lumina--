"use client";
import {useRef,useState} from "react";
type VoiceState="idle"|"asking"|"listening"|"searching"|"error";type VoiceEvent={results:{[index:number]:{[index:number]:{transcript:string}}}};type Recognition={lang:string;interimResults:boolean;continuous:boolean;onresult:((event:VoiceEvent)=>void)|null;onerror:(()=>void)|null;onend:(()=>void)|null;start:()=>void};type Constructor=new()=>Recognition;
function setInput(input:HTMLInputElement,value:string){const setter=Object.getOwnPropertyDescriptor(HTMLInputElement.prototype,"value")?.set;setter?.call(input,value);input.dispatchEvent(new Event("input",{bubbles:true}))}
function speak(text:string,onEnd?:()=>void){if(!("speechSynthesis" in window)){onEnd?.();return}window.speechSynthesis.cancel();const message=new SpeechSynthesisUtterance(text);message.rate=.96;message.pitch=.92;message.onend=()=>onEnd?.();message.onerror=()=>onEnd?.();window.speechSynthesis.speak(message)}
export default function VoiceSearchOrb({left,top}:{left:number;top:number}){const [state,setState]=useState<VoiceState>("idle"),[caption,setCaption]=useState("Tap to ask YNOT"),recognition=useRef<Recognition|null>(null);
function search(query:string){const clean=query.trim();if(clean.length<2){setState("error");setCaption("I didn’t catch that. Tap again.");return}setState("searching");setCaption(`Searching “${clean}”`);const input=document.querySelector<HTMLInputElement>(".lv4-search input");if(input){setInput(input,clean);requestAnimationFrame(()=>document.querySelector<HTMLButtonElement>(".lv4-search button")?.click())}window.setTimeout(()=>{setState("idle");setCaption("Tap to ask YNOT")},2200)}
function begin(){if(state==="asking"||state==="listening"||state==="searching")return;const scope=window as unknown as {SpeechRecognition?:Constructor;webkitSpeechRecognition?:Constructor},VoiceRecognition=scope.SpeechRecognition||scope.webkitSpeechRecognition;if(!VoiceRecognition){setState("error");setCaption("Voice unavailable—use search below");document.querySelector<HTMLInputElement>(".ynot-bottom-search input")?.focus();return}const listener=new VoiceRecognition();recognition.current=listener;listener.lang=navigator.language||"en-US";listener.interimResults=false;listener.continuous=false;listener.onresult=event=>{const words=event.results[0]?.[0]?.transcript||"";recognition.current=null;speak(`Searching for ${words}`,()=>search(words))};listener.onerror=()=>{recognition.current=null;setState("error");setCaption("Microphone unavailable. Tap to retry.")};listener.onend=()=>{if(recognition.current){recognition.current=null;setState(current=>current==="listening"?"idle":current)}};setState("asking");setCaption("YNOT is asking…");speak("What would you like me to find?",()=>{try{setState("listening");setCaption("Listening…");listener.start()}catch{setState("error");setCaption("Tap again to allow microphone access")}})}
return <button className={`ynot-voice-orb ${state}`} style={{left,top}} onClick={begin} aria-label="Ask YNOT to search by voice" aria-live="polite"><img src="/ynot-microphone.jpg" alt=""/><span className="ynot-voice-pulse"/><strong>{caption}</strong></button>}