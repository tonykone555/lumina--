"use client";

import {useEffect,useRef} from "react";
import {dynamicLuminaPrice} from "@/lib/commerce/engine";

type Variant={id?:string;label?:string;price?:number|null;currency?:string;image?:string;url?:string;available?:boolean};
type Product={id:string;variantId?:string;title:string;brand?:string;price:number|null;currency?:string;image?:string;images?:string[];url?:string;source?:string;category?:string;variants?:Variant[];description?:string;supplierPrice?:number};
type RichResponse={url?:string;variantId?:string;product?:Product;error?:string};
const products=new Map<string,Product>();const pending=new Map<string,Promise<Product>>();
function k(s:string){return String(s||"").toLowerCase().replace(/[^a-z0-9]+/g," ").trim()}
function isShopify(p?:Product){return Boolean(p?.source?.toLowerCase().includes("shopify"))}
function money(v:number,currency="EUR"){try{return new Intl.NumberFormat(undefined,{style:"currency",currency,maximumFractionDigits:2}).format(v)}catch{return`${v.toFixed(2)} ${currency}`}}
function inferCategory(p:Product){const text=`${p.category||""} ${p.title||""}`.toLowerCase();if(/dress|fashion|shirt|shoe|bag|jewel|accessor|apparel|clothing/.test(text))return"fashion";if(/fitness|gym|training|running|recovery|sport/.test(text))return"fitness";if(/skin|beauty|serum|cream|spf|cleanser/.test(text))return"skin";if(/hair|scalp|shampoo|conditioner/.test(text))return"hair";if(/home|decor|furniture|lamp|kitchen|bedding/.test(text))return"home";if(/tech|phone|audio|headphone|charger|gaming|smart/.test(text))return"tech";return"default"}
function retail(p:Product,supplier?:number|null){const base=Number(supplier??p.supplierPrice??p.price);if(!Number.isFinite(base)||base<=0)return null;return dynamicLuminaPrice({sourceId:p.source||"discovery",productId:p.variantId||p.id,title:p.title,category:inferCategory(p),price:base,currency:p.currency||"EUR",shipping:0,stockConfidence:.75,returnPolicyScore:.7,regionMatch:.75})}
async function hydrate(product:Product){const id=k(product.title);const old=pending.get(id);if(old)return old;const job=(async()=>{if(!isShopify(product))return product;const r=await fetch("/api/commerce/product-link",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(product)});const data=await r.json() as RichResponse;if(!r.ok||!data.product)throw new Error(data.error||"PRODUCT_DETAILS_UNAVAILABLE");const rich={...product,...data.product,url:data.url||data.product.url||product.url};products.set(id,rich);return rich})().finally(()=>pending.delete(id));pending.set(id,job);return job}
function currentProduct(title:string){return products.get(k(title))}
function render(root:HTMLElement,p:Product){if(root.dataset.ynotHydrated===String(p.id)&&root.querySelector(".ynot-rich-gallery"))return;root.dataset.ynotHydrated=String(p.id);
 const shell=root.closest(".lv4-detail") as HTMLElement|null;if(!shell)return;const main=shell.querySelector<HTMLImageElement>(":scope > img");
 const oldGallery=shell.querySelector(".lv4-gallery");oldGallery?.remove();shell.querySelector(".ynot-rich-gallery")?.remove();
 const images=[...new Set([p.image,...(p.images||[]),...(p.variants||[]).map(v=>v.image).filter(Boolean)].filter(Boolean) as string[])];
 if(images.length>1){const gallery=document.createElement("div");gallery.className="ynot-rich-gallery";for(const src of images.slice(0,12)){const b=document.createElement("button");b.type="button";const img=document.createElement("img");img.src=src;img.alt="";b.appendChild(img);b.addEventListener("click",e=>{e.preventDefault();e.stopPropagation();if(main)main.src=src;gallery.querySelectorAll("button").forEach(x=>x.classList.toggle("active",x===b))});gallery.appendChild(b)}main?.insertAdjacentElement("afterend",gallery)}
 root.querySelector(".lv4-variants")?.remove();root.querySelector(".ynot-rich-variants")?.remove();
 if((p.variants||[]).length){const variants=document.createElement("div");variants.className="ynot-rich-variants";for(const v of (p.variants||[]).slice(0,24)){const b=document.createElement("button");b.type="button";b.disabled=v.available===false;b.textContent=v.label||"Option";if(String(v.id||"")===String(p.variantId||""))b.classList.add("active");b.addEventListener("click",e=>{e.preventDefault();e.stopPropagation();variants.querySelectorAll("button").forEach(x=>x.classList.remove("active"));b.classList.add("active");shell.dataset.ynotVariantId=String(v.id||"");const next={...p,variantId:v.id,price:v.price??p.price,supplierPrice:v.price??p.supplierPrice??p.price,currency:v.currency||p.currency,image:v.image||p.image,url:v.url||p.url};products.set(k(p.title),next);if(v.image&&main)main.src=v.image;const strong=root.querySelector<HTMLElement>("strong");const rp=retail(next,v.price);if(strong&&rp!=null)strong.textContent=money(rp,next.currency||"EUR")});variants.appendChild(b)}const actions=root.querySelector(".lv4-actions");actions?.parentElement?.insertBefore(variants,actions)}
 let flip=root.querySelector<HTMLButtonElement>(".ynot-description-toggle");if(!flip){flip=document.createElement("button");flip.type="button";flip.className="ynot-description-toggle";flip.textContent="Description";root.appendChild(flip)}
 let back=shell.querySelector<HTMLElement>(".ynot-description-back");if(!back){back=document.createElement("section");back.className="ynot-description-back";const close=document.createElement("button");close.type="button";close.className="ynot-description-close";close.textContent="Back";const label=document.createElement("small");label.textContent="PRODUCT DESCRIPTION";const h=document.createElement("h3");h.textContent=p.title;const copy=document.createElement("p");copy.className="ynot-description-copy";back.append(close,label,h,copy);shell.appendChild(back);close.addEventListener("click",e=>{e.preventDefault();e.stopPropagation();shell.classList.remove("ynot-description-flipped")})}
 const copy=back.querySelector<HTMLElement>(".ynot-description-copy");if(copy)copy.textContent=p.description?.trim()||"No merchant description was supplied for this product.";
 flip.onclick=e=>{e.preventDefault();e.stopPropagation();shell.classList.add("ynot-description-flipped")};
 root.querySelectorAll(".lv4-tagrow button").forEach(btn=>{const text=(btn.textContent||"").trim();if(/[€$£]\s?\d|\d+[.,]\d{2}/.test(text))btn.remove()});
}

export default function ProductDetailHydrator(){const observer=useRef<MutationObserver|null>(null);useEffect(()=>{const ingest=(event:Event)=>{const detail=(event as CustomEvent<Product>).detail;if(detail?.title)products.set(k(detail.title),detail)};window.addEventListener("ynot:product-seen",ingest as EventListener);
 const scan=()=>{document.querySelectorAll<HTMLElement>(".lv4-detailcopy").forEach(async root=>{const title=root.querySelector("h2")?.textContent?.trim()||"";if(!title)return;let p=currentProduct(title);if(!p){const bubble=[...document.querySelectorAll<HTMLElement>(".lv4-product")].find(x=>k(x.querySelector(".lv4-product-tooltip b")?.textContent||"")===k(title));if(bubble){const raw=(bubble as any).__ynotProduct as Product|undefined;if(raw){products.set(k(title),raw);p=raw}}}if(!p)return;try{const rich=await hydrate(p);if(document.body.contains(root))render(root,rich)}catch{if(document.body.contains(root))render(root,p)}})};
 const onClick=(e:Event)=>{const bubble=(e.target as HTMLElement)?.closest(".lv4-product") as HTMLElement|null;if(!bubble)return;const title=bubble.querySelector(".lv4-product-tooltip b")?.textContent||"";const p=currentProduct(title);if(p)setTimeout(()=>hydrate(p).then(r=>{const root=document.querySelector<HTMLElement>(".lv4-detailcopy");if(root&&k(root.querySelector("h2")?.textContent||"")===k(title))render(root,r)}).catch(()=>{}),0)};
 document.addEventListener("click",onClick,true);observer.current=new MutationObserver(()=>queueMicrotask(scan));observer.current.observe(document.body,{subtree:true,childList:true});scan();return()=>{window.removeEventListener("ynot:product-seen",ingest as EventListener);document.removeEventListener("click",onClick,true);observer.current?.disconnect()}},[]);return null}
