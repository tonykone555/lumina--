name: Deploy Modal video worker

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "modal/**"
      - ".github/workflows/deploy-modal-video.yml"

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install "modal>=1.2,<2"
      - name: Deploy YNOT video worker
        env:
          MODAL_TOKEN_ID: ${{ secrets.MODAL_TOKEN_ID }}
          MODAL_TOKEN_SECRET: ${{ secrets.MODAL_TOKEN_SECRET }}
        run: modal deploy modal/ynot_video_worker.py --name ynot-video-worker
