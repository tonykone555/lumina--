(()=>{let last=0;function open(){const now=Date.now();if(now-last<250)return;last=now;window.dispatchEvent(new CustomEvent('ynot:open-auth',{detail:{mode:'signin',profile:true}}))}function handler(e){const target=e.target instanceof Element?e.target.closest('.ynot-reference-avatar'):null;if(!target)return;if(e.type==='pointerup'||e.type==='touchend'){e.preventDefault();e.stopPropagation()}open()}document.addEventListener('pointerup',handler,true);document.addEventListener('click',handler,true)})();